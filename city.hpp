#ifndef GENETICALGORITHM_CITY_HPP
#define GENETICALGORITHM_CITY_HPP
#include <string>

using namespace std;

class city {
private:
    string name;
    long x;
    long y;

public:
    city(string, long, long);
};


#endif //GENETICALGORITHM_CITY_HPP
